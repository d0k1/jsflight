/**
 * Базовый скрипт обработки скриптовых событий
 * Скрипту доступны след. переменные:
 *      event - событие
 *      logger - логгер
 *      classloader - classloader от ScriptEngine
 *      playerContext - контекст плеера
 */

return;