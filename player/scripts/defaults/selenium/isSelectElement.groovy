/**
 * Базовый скрипт определения, является ли WebElement элементом типа select
 * Скрипту доступны след. переменные:
 *      element - WebElement, сам элемент
 *      webdriver - вебдрайвер
 *      logger - лог
 */

return false;